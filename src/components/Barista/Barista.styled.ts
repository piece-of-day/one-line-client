import styled from 'styled-components';
import { motion } from 'framer-motion';

export const BaristaWrapper = styled.div`
  width: 15rem;
`;

export const BaristaImg = styled(motion.img)`
  width: 100%;
`;
